// Supabase desativado neste commit por segurança.
// Não usar chaves hardcoded; integrar via variáveis de ambiente (Vite) quando habilitar.
// export const supabase = createClient<VITE_DB>(import.meta.env.VITE_SUPABASE_URL, import.meta.env.VITE_SUPABASE_ANON_KEY)

// Placeholder para evitar uso acidental.
export const supabase: any = null;